# API Keys (secrets - keep in .env, NOT in config.json)
GEMINI_API_KEY="your_gemini_api_key_here"
OPENAI_API_KEY="your_openai_api_key_here"

# Email credentials (secrets)
EMAIL_SENDER="pkkolla24@gmail.com"
EMAIL_APP_PASSWORD="your_gmail_app_password_here"

# API Gateway trigger token (generate with: python -c "import uuid; print(uuid.uuid4())")
TRIGGER_TOKEN="your_random_uuid_token_here"

# All other settings (model names, search keywords, location, job limit, etc.)
# are now managed in config.json and editable via the web dashboard.
